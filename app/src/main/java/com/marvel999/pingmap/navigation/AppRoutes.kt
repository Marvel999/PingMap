package com.marvel999.pingmap.navigation

/**
 * Routes not shown on the bottom bar (e.g. opened from Home).
 */
object AppRoutes {
    const val HISTORY = "history"
    const val TOOLS = "tools"
    const val DEVICES = "devices"
}
